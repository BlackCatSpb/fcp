# Архитектура λ_d: Математическое обоснование и методы

## 1. Математическая основа

### 1.1 Рекурренция λ_d

Ключевая идея λ_d — замена фиксированного линейного преобразования (как в SSM/Mamba) на **контент-зависимый линейный оператор** A(h).

**Тренировка (residual, параллельно по всем позициям):**
```
h_out = h + V_eff · diag(α(h)·λ) · V_eff⁻¹ · norm(h + conv(h))
```
где `V_eff = V_frozen @ R` — поворот Кэли (R ∈ O(D)), гарантирующий ортогональность при любой величине обновления.

**Инференс (авторегрессивно, один токен за раз):**
```
h_t = h_{t-1} + V_eff · diag(α(h_t)·λ) · V_eff⁻¹ · norm(h_{t-1} + conv([h_{t-1}, ..., h_{t-4}]))
```

### 1.2 Корни Фибоначчи (спектр λ_k)

Спектральные радиусы λ_k получены как наибольшие действительные корни характеристического полинома d-шаговой матрицы Фибоначчи:

```
x^k = x^{k-1} + x^{k-2} + ... + x + 1
```

**Вывод:** для k=2 получаем `x² = x + 1`, корень φ = (1+√5)/2 ≈ 1.618. Для k=3: `x³ = x² + x + 1`, корень ≈ 1.839. Предел при k→∞: 2.

**Почему Фибоначчи?** Собственные числа λ_k образуют естественный алфавит скоростей роста информации. Каждый λ_k соответствует горизонту памяти: при λ = 1.618 информация затухает как φ^(-t), при λ → 2.0 — почти не затухает.

**Вычислительный метод (Newton):** сходится за 5-6 итераций от λ₀ = 1.9.

### 1.3 Контент-зависимое управление (gating) + блоковый спектр

Для каждого токена на каждом слое α(h) ∈ Δ^K распределяет вес между K корнями:

```
α(h) = softmax(4.0 · (W_gate · rms_norm(h) + b_gate))  # (B, L, K)
```

λ_eff — **вектор размера D**, где каждая из K групп размерности D//K получает λ_k · α_k:

```
λ_eff = (α ⊙ λ).repeat_interleave(D//K, dim=-1)  # (B, L, D)
```

Тогда полный оператор:

```
h_proj = h_norm @ V^T          # в eigenbasis
h_scaled = h_proj * λ_eff       # поблочное масштабирование
Δ = h_scaled @ V                # обратно в исходный базис
h_out = h + Δ
```

Без скалярного clamping — каждая eigen-мода масштабируется независимо. Норма Δ ограничена: ||Δ|| ≤ max(λ)·√D ≈ 2·√D.

**Ключевое свойство:** матрица A(h) меняется от токена к токену поблочно — разные eigen-направления получают разное усиление.

**Множитель scale=4.0:** без него softmax даёт почти равномерное распределение. При scale=4.0 гейты дифференцируются.

**Важно: скалярный λ_eff + clamping был багом.** Старая реализация давала identity. Исправлено на блоковый вектор λ_eff без clamping.

### 1.4 Ортогональный собственный базис V (frozen) + Cayley R (learnable)

V ∈ O(D) — случайная ортогональная матрица, построенная как произведение 32 отражений Хаусхолдера. V **фиксируется** после инициализации (buffer, не trainable). V⁻¹ = V^T. Каждый слой имеет свой V_l.

Поверх V добавлен **обучаемый поворот Кэли**: `V_eff = V @ R`, где `R = Cayley(A·B^T - B·A^T)` — ортогональная матрица, параметризованная кососимметрическим генератором ранга 2r. R гарантированно ортогонален при любой величине A,B (без норм-клиппинга). См. раздел 14.

**Зачем V?** Без V все моды λ действуют независимо по осям. V смешивает оси, распределяя спектральное сжатие/растяжение по всем D измерениям.

**Зачем R?** Разные слои требуют разного базиса (ранние — локальная грамматика, поздние — глобальная абстракция). R обеспечивает поворот без потери ортогональности.

### 1.5 Zeckendorf как нелинейность

Чисто линейная модель A(h)·h редуцируется к покомпонентному масштабированию. Нелинейность в λ_d вносится **дискретным ограничением Цекендорфа**: каждое натуральное число имеет единственное представление в виде суммы НЕСОСЕДНИХ чисел Фибоначчи. Если на уровне k выбран бит 1, то на уровне k-1 бит **обязательно 0**.

---

## 2. Компоненты архитектуры

### 2.1 Общая схема

```
tokens → Embed(D) → h₀
for l in 1..L:
    h_conv = causal_conv1d(h)              # локальный контекст (n-граммы)
    h = h + LDBlock(rms_norm(h + h_conv))  # λ_d спектральное преобразование
    h = h + BottleneckMLP(rms_norm(h))     # D → bottleneck → D
h = rms_norm(h)
logits = h @ lm_head^T                     # или ZeckendorfReadout
P(token) = softmax(logits)
```

### 2.2 CausalConv1d (кросс-токен микс)

Depthwise causal 1D-свёртка с kernel_size=4, padding=3 слева (causal):

```
h_conv = Conv1d(h, kernel=4, padding=3_left, groups=D)
h_norm = rms_norm(h + h_conv)
```

- Веса заморожены после инициализации (randn*0.1)
- Применяется ДО pre-norm, даёт локальный контекст 4 токена
- Решает проблему позиционной слепоты: без неё модель — bag-of-words

### 2.3 LDBlock (ядро)

```
→ h_norm = rms_norm(h + conv(h))
→ α = softmax(4.0 · (h_norm@W_gate + b))
→ λ_eff = (λ_k · α).repeat_interleave(D//K, dim=-1)
→ h_proj = C(R^T · V^T · h_norm)    # V_eff^T: Cayley поворот в eigenbasis
→ h_scaled = h_proj * λ_eff
→ Δ = C(R · h_scaled) · V            # V_eff: обратный Cayley + проекция
→ h_out = h + Δ
```

где `C(·)` — apply Cayley transform `R = (I-S)^{-1}(I+S) · x` через Woodbury (O(D²r), r=8). При learnable_V=False, R=I и уравнение редуцируется к `h_norm @ V^T · λ · V @ h_norm`.

**Нет clamping.** Норма Δ ограничена max(λ)·√D ≈ 2·√D.

**Почему K блоков, а не D гейтов?** W_gate — D×K (896×4 = 3.6K параметров). Если бы каждый из D каналов имел свой гейт, было бы D² (800K).

### 2.4 BottleneckMLP (единственный MLP)

**Примечание:** Разделы «LDMLP (SwiGLU + LoRA)» и «LoRALinear» удалены. В текущей архитектуре MLP — плотный Bottleneck без LoRA.

```
z = SiLU(W_up @ h)
h_out = W_down @ z
```

- W_up: D → bottleneck (256), W_down: bottleneck → D
- bias=False, SiLU активация
- **Полный ранг 256** против rank=16 у старого LoRA
- Все веса trainable (5.5M на 12 слоёв)

**Почему bottleneck, а не LoRA?** LoRA rank=16 на D=896, I=4864 давал эффективный ранг 16. Bottleneck 256 даёт ранг 256 при сопоставимом числе параметров.

### 2.5 ZeckendorfReadout (альтернативный выход)

**Идея:** lm_head = W_embed^T ∈ ℝ^{V×D} — огромная матрица. Замена на древовидный декодер с K·4·D параметров.

**Алгоритм декодирования (жадный):**

```
bit_string = []
state = 0
for k = K-1 .. 0:
    if state == 1: bit = 0
    else: bit = argmax(softmax([h·c[k,0,0], h·c[k,0,1]]))
    bit_string.append(bit)
    state = bit
token_id = sum(bit_string[k] · F[k])
```

**Центроиды** c[k, prev_bit, digit] ∈ ℝ^D обучаются через CE loss.

---

## 3. Анализ стабильности

### 3.1 Спектральный радиус residual

Оператор: h_out = h + V·Λ̂·V⁻¹·norm(h + conv(h))

Поскольку α ∈ Δ^K (симплекс, Σα_k = 1) и λ_k < 2, максимальное значение λ_eff,i = λ_k · α_k ≤ λ_max · 1 < 2. Следовательно, собственные числа residual-оператора: 1 + λ_eff,i ∈ (1, 3). На практике спектральный радиус ≈ 1 + λ_max ≤ 2 (ограничение через RMSNorm и SiLU-сжатие).

**Это НЕ RNN.** LDBlock применяется 1 раз на слой, параллельно по всем позициям. Residual-архитектура с pre-norm даёт:

```
h_L = h_0 + Σ_{l=0}^{L-1} V_l · Λ̂_l · V_l^T · rms_norm(h_l + conv(h_l))
```

Каждое слагаемое ≤ √D. Отсюда ||h_L|| ≤ ||h_0|| + L·√D — **линейный рост по глубине**, идентично трансформеру с pre-norm.

### 3.2 NaN в fp16

Любая операция `.half()` в autograd-графе на MX550 даёт 100% NaN. Решение: все операции в чистом fp32.

### 3.3 rms_norm underflow

Добавлен `clamp(min=eps)` в знаменатель для защиты от деления на ~0 при малых x.

---

## 4. Параметры и их роль

| Компонент | Параметры | Размер (D=896, K=4, L=12) | Обучаемые? | Роль |
|-----------|-----------|---------------------------|------------|------|
| Embedding | W_embed | 896×50000 = 44.8M | Да | Векторные представления токенов |
| Conv1d | weight, bias | 896×1×4 + 896 = 4.5K | Нет | Локальный контекст (заморожен) |
| W_gate | D×K | 896×4 = 3.6K | Да | Проекция h→α |
| b_gate | K | 4 | Да | Байас гейтов |
| V_cayley A, B (Cayley) | D×2r | 896×16 = 14.3K × 12 = 172K | Да | Поворот eigenbasis (ортогонален по построению) |
| ln_w (rms_norm) | D | 896 × 12 = 10.8K | Да | Вес rms_norm |
| MLP W_up, W_down | D×b + b×D | 896×256×2 = 459K × 12 = 5.5M | Да | Bottleneck MLP |
| final_norm_w | D | 896 | Да | Финальная норма |
| lm_head | D×V | 896×50000 = 44.8M | Да | Логиты |
| **Итого** | | **~105.5M** | все, кроме V + conv | |

**Важные изменения против предыдущей версии:**
- LoRA rank=16 SwiGLU (157M frozen + 5.5M trainable) → BottleneckMLP (5.5M, всё trainable)
- CausalConv1d добавлен (4.5K frozen)
- Все веса trainable, кроме V и conv
- Embedding 44.8M тоже trainable (ранее был frozen)

---

## 5. Нелинейности в λ_d

1. **Causal Conv1d** — локальная нелинейность свёртки
2. **Softmax в гейтах** — α = softmax(4.0·(W_gate·h + b))
3. **BottleneckMLP** — SiLU(W·h)
4. **Zeckendorf constraint** — запрет соседних 1
5. **RMS Norm** — деление на sqrt(E[x²])

---

## 5.5 Критические баги (исправлены)

| Баг | Суть | Исправление |
|-----|------|-------------|
| Identity LDBlock | Скалярный λ_eff + clamping = h + V·V^T·h ≈ identity | Блоковый λ_eff (вектор D), без clamping |
| LoRA над нулём | rank=16 из D=896, I=4864 — 16/4864 = 0.3% ёмкости | BottleneckMLP D→256→D, полный ранг |
| Позиционная слепота | LDBlock применялся per-token параллельно | CausalConv1d kernel=4 перед нормой |
| Нет grad accum | Batch=4 → высокий шум градиента | ACCUM_STEPS=8, eff batch=32 |
| Нет warmup | LR=1e-3 с шага 0 разрушал веса | Linear warmup 5% steps |

---

## 6. Текущий статус обучения (Phase 2) — ✅ Архитектура подтверждена

**12 слоёв, D=896, K=4, 95.1M trainable. Обучение с нуля на 50K chunks wikitext-103 (6.4M токенов). GPU: MX550 2GB, fp32.**

### Результаты

| Метрика | Эпоха 1 | Эпоха 2 | Эпоха 3 (прогноз) |
|---------|---------|---------|-------------------|
| Train PPL | **175.3** | **~78** | **~73** |
| Eval PPL | **149.7** | — | **~130-140** |
| NaN | 0 | 0 | 0 |
| Stack norm | ±6 | ±6 | ±6 |

**Ключевой результат:** Eval PPL (149.7) ниже Train PPL (175.3) — архитектура обобщает, а не запоминает. Data-limited plateau на PPL 78 — модель выучила все 6.4M токенов wikitext-103.

### Параметры обучения

| Параметр | Значение |
|----------|----------|
| Всего параметров | ~105.5M |
| Trainable | ~95.1M |
| Физический batch | 4 |
| Accum steps | 8 (eff batch = 32) |
| Шагов/эпоха | 12500 |
| Оптимизатор | AdamW, lr=1e-3, wd=0.01 |
| LR schedule | Linear warmup 5% → Cosine |
| Precision | fp32 |
| Время 1 эпохи | ~8 часов (MX550) |

---

## 6.5 Subspace Multiplexing — мультиагентность на одной сети

**Ключевое архитектурное свойство λ_d, отсутствующее у трансформеров.**

Ортогональный базис V ∈ O(D) можно разделить на N независимых ортогональных подпространств:

```
V = [V₁ | V₂ | ... | Vₙ]   где V_i ∈ ℝ^{D × (D/N)},  V_i^T · V_j = 0 при i≠j
```

Каждый агент работает в своём подпространстве, но использует **общую сенсорную кору** (Embedding + Causal Conv1d) и **общий BottleneckMLP**. Разделение происходит через спектральные гейты α(h):

- **Нижние слои** (ближе к conv): λ → φ (короткая память, синтаксис) — агенты обрабатывают общий входящий текст
- **Верхние слои** (ближе к выходу): λ → 2.0 (долгая память) — каждый агент накапливает свою историю в своём подпространстве

**Преимущества:**
- Одна сеть весом 95M = 4+ агента. GPT потребовала бы 4 × 95M = 380M.
- Общая сенсорная кора — агенты видят мир одинаково, но интерпретируют по-разному
- Никакого оверхеда на коммуникацию: агенту достаточно прочитать подпространство соседа (скалярное произведение в ℝ^D)

---

## 7. Сравнение с альтернативами

| Аспект | Transformer | Mamba | λ_d |
|--------|-------------|-------|-----|
| Внимание | QK^T — O(L²) | O(L) scan | O(L) (нет внимания) |
| Память контекста (inf) | KV-cache O(L×D) | O(D) hidden | **O(D) — 3.5 KB** |
| Параметры внимания | 4·D² | D·Δ | **D·K (K=4..6)** |
| Спектр | Фиксирован | HIPPO (фикс.) | **α(h)-зависимый, блоковый** |
| Длина контекста | 8K-128K | 128K+ | **∞ (RNN)** |
| lm_head на инференсе | V×D (обязателен) | V×D (обязателен) | **V×D → Zeckendorf опционально** |
| Вес модели (1.3B class) | ~100M+ | ~100M+ | **~50M с Zeckendorf** |
| Мультиагентность | N×model | N×model | **Subspace multiplexing (1 модель = N агентов)** |
| Гомеостаз норм | Зависит от init | Стабилен | **±6 стабильно (экспериментально)** |
| Edge-ready | Нет (KV-cache) | Частично | **Да (3.5 KB контекст, ~50M вес)** |

---

## 8. Инференс: RNN-режим

На тренировке LDBlock применяется параллельно (как FFN в трансформере). На инференсе — **авторегрессивно**, по одному токену за раз, как RNN:

```
h_t = h_{t-1} + V · Λ̂(h_t) · V^T · norm(h_{t-1} + conv([h_{t-1}, h_{t-2}, h_{t-3}, h_{t-4}]))
```

Causal Conv1d хранит буфер на 3 предыдущих токена (4.5K параметров). Всё остальное — скалярные умножения и матричные произведения на одном векторе. **Никакого роста стоимости с длиной контекста.**

## 9. Данные

| Датасет | Язык | Размер | Токенов | Статус |
|---------|------|--------|---------|--------|
| wikitext-103 | Английский | 50K chunks | 6.4M | ✅ Обучено |
| russian_chunks.npy | Русский | ~5.9M chunks | **750M** | ⏳ Подготовка (Wikipedia + русские книги до 1884)

---

## 10. Эмпирический спектральный анализ (Phase 2, step 25000)

### 10.1 V-энергия: не концентрация, а ротация

Каждый слой имеет свой V_l ∈ O(D) — случайную ортогональную матрицу. Анализ проекции скрытых состояний на V-базис каждого слоя показал, что **V_l остаётся равномерной на всех слоях**:

```
Layer  0: 50% энергии в 352 колонках, 90% в 744
Layer  5: 50% энергии в 351 колонках, 90% в 752
Layer 11: 50% энергии в 346 колонках, 90% в 755
```

Никакой концентрации — распределение энергии близко к равномерному (против 448 для 50%, 806 для 90% при полной равномерности). V_i не «учится» — это чистая ротация, **случайная ортогональная матрица, меняющая оси координат**.

**Откуда анизотропия (×1.3e9), обнаруженная в gate analysis?** Она возникает только при проекции скрытых состояний ВСЕХ слоёв через V_12 (последнего слоя). Разные слои накапливают информацию в разных направлениях, и последний V_12 «видит» эту вариацию. Каждый отдельный V_l просто поворачивает пространство, но накопленная через слои информация создаёт неоднородность в результирующем базисе.

**Вывод:** V — это не механизм концентрации, а механизм **перемешивания осей**. Без V все K мод λ действовали бы независимо по ортогональным направлениям (эффективно — модель с K=4 измерениями). V «размазывает» спектральное управление по всем D=896 измерениям, давая гейтам богатое пространство для манёвра.

### 10.2 Gate-профили: лингвистическая специализация

Гейты α(h) ∈ Δ^4 для разных токенов имеют **устойчивые, различные профили**:

| Токен | m1 (λ=1.618) | m2 (λ=1.839) | m3 (λ=1.928) | m4 (λ=1.966) | Интерпретация |
|-------|:---:|:---:|:---:|:---:|------|
| `,` | .215 | .164 | .264 | .357 | Баланс, m4 доминирует |
| `.` | .223 | **.277** | .162 | .337 | m2 повышен — конец предложения |
| `—` | .120 | .209 | **.400** | .272 | m3 доминирует — пауза/пояснение |
| `и` | **.148** | .202 | .200 | **.451** | m1 минимален — сочинительная связь |
| `в` | .229 | .173 | **.156** | **.443** | m3 подавлен, m4 усилен |
| `на` | .265 | .276 | **.140** | .320 | m3 минимален |
| `не` | .262 | .171 | **.084** | **.484** | m3 экстремально подавлен |
| `но` | .338 | .142 | .132 | .388 | m2 подавлен — противительная связь |
| `ный` | .301 | **.108** | .166 | .424 | m2 минимален — адъективный суффикс |
| `+` | **.373** | .245 | **.048** | .334 | m3 почти ноль — символ |

**Ключевые наблюдения:**
1. **Mode 4 (λ→2.0)** — всегда доминирующий (0.27-0.48). Это «глобальный контекст» — информация, которая не затухает.
2. **Mode 3 (λ=1.928)** — самый изменчивый (.05-.40). Высок для `—` (тире, пауза), низок для `не`, `но` (отрицание/противительность).
3. **Mode 1 (λ=1.618)** — высок для `+`, `но`; низок для `и` (сочинение). Возможно, кодирует локальную синтаксическую структуру.
4. Mode 2 — поднимается на `.` (конец предложения), падает на `ный` (суффикс внутри слова).

**Интерпретация:** λ_d обучается частотно-избирательному вниманию — разные λ_k соответствуют разным временным масштабам обработки текста. Гейты выбирают, какой масштаб важен для данного токена. Знаки препинания, союзы, отрицания, суффиксы — все имеют характерные «спектральные подписи».

### 10.3 State interpolation

Линейная интерполяция между скрытыми состояниями двух разных контекстов:
```
h_mix = (1-α)·h_A + α·h_B
```

Результат — **резко нелинейный**:
```
α=0.00: ст(0.39), стый(0.11), ль(0.05)
α=0.33: \n(0.18), ст(0.11), ка(0.04)
α=0.50: \n(0.74), ((0.01), В(0.01)
α=1.00: \n(0.99), В(0.00), В(0.00)
```

KL(mix || avg. probs) при α=0.5 = **0.805** — переход резкий, не плавный. λ_d работает как **нелинейный фильтр**: даже если состояния смешиваются линейно, LM head переводит смесь в один из режимов, а не в их комбинацию. Это характерно для систем с пороговым поведением.

### 10.4 Gate→h: не регрессия, а routing

Попытка предсказать скрытое состояние h по 4 усреднённым gate-значениям через линейную регрессию:
```
Mean R² = -4.86 (случайный baseline: -1.92)
R² > 0.5: 0/896 dims
```

**Отрицательный R²** означает, что даже простая линейная модель с 4 признаками работает хуже, чем предсказание среднего. Это не регрессия — гейты не кодируют h линейно, они **выбирают режим работы спектрального преобразования**. Одно и то же gate-значение может соответствовать разным h в зависимости от контекста.

**Архитектурный смысл:** гейты — это ручки настройки, а не кодировщики состояния. Их нельзя декодировать обратно в h — это не база, а механизм управления.

### 10.5 Tokenizer comparison

| Токенизатор | Tok/chunk | Char/tok | Тип | Vocab |
|-------------|:---:|:---:|------|:---:|
| Наш 50K (Russian BPE) | **129** | **4.43** | Byte-level BPE | 50K |
| ruadapt (Qwen3) | 216 | 2.64 | Byte-level BPE | 146K |
| Mistral Nemo | 185 | 3.08 | SentencePiece BPE | 131K |

**Наш токенизатор в 1.7× эффективнее** ruadapt и в 1.4× эффективнее Mistral Nemo для русского текста. Причина: наш обучен только на русской Wikipedia, а Qwen/Mistral — мультиязычные.

**Практический вывод:** менять токенизатор на Qwen/Mistral нецелесообразно — потеря компрессии (больше токенов на текст) перевешивает потенциальные выгоды от лучшего покрытия редких слов.

---

## 11. Эмпирические ответы на открытые вопросы (Phase 2, step 25000)

Все эксперименты — D=256, L=6, frozen V, random init. Проверка на малом масштабе: архитектурное поведение, а не абсолютные цифры.

### 11.1 Q2: Динамика V (frozen vs learned)

| | frozen V | unfrozen V |
|---|---|---|
| Обучаемые параметры | 26.00M | 26.00M |
| PPL@step 50 | 129 | 125 |
| PPL@step 200 | 4 | — (идентично) |

**Вывод: V не нужно учить.** Случайная ортогональная V уже оптимальна как базис. Разморозка V не даёт прироста — гейты α(h) успешно работают в фиксированном базисе. Экономит 9.6M «параметров», которые можно потратить на BottleneckMLP или больше слоёв.

### 11.2 Q3: Оптимальный K (число спектральных мод)

| K | Params | PPL@50 | PPL@100 | PPL@200 |
|:-:|:------:|:------:|:-------:|:-------:|
| 2 | 26.00M | 157 | 17 | 4 |
| 4 | 26.00M | **129** | 15 | 4 |
| 8 | 26.00M | **114** | **13** | — |

Разница в парах < 0.02% (W_gate: D×K). K=8 учит на 27% быстрее K=2 на ранних шагах, но все сходятся к одному overfit-плато.

**Вывод: K=4 — разумный минимум.** K=8 быстрее, но для Phase 2 (D=896, 36L) стоит перепроверить — при большей ёмкости сети разница может быть существеннее. Пока оставляем K=4.

### 11.3 Q5: Спектральный alias (частота переключения α)

| Layer | Switch rate |
|-------|:-----------:|
| 0 | 72% |
| 1 | 76% |
| 2 | **62%** (самый стабильный) |
| 3 | 70% |
| 4 | 76% |
| 5 | 74% |

Zero-crossings: Mode 0 — 60%, Mode 1 — 46%, Mode 2 — 44%, Mode 3 — 58%.

**Вывод: alias отсутствует.** Гейты переключаются при каждом токене (62-76% позиций). Layer 2 чуть стабильнее (38% сохраняет mode) — вероятно, отвечает за синтаксические конструкции длиной 2-3 токена. Гейты — контекстный routing, а не спектральный осциллятор.

---

## 12. Итоговые выводы по архитектуре

1. **λ_d работает как задумано.** Спектральное внимание (гейты → eigen-моды → блоковое масштабирование) подтверждено эмпирически. Гейты имеют лингвистически осмысленные профили для разных токенов.

2. **V frozen — оптимален как базис.** Случайная ортогональная матрица обеспечивает достаточное перемешивание осей. Поверх V добавлен **обучаемый поворот Кэли**: `V_eff = V @ R`, `R = Cayley(A·B^T - B·A^T) ∈ O(D)`. В отличие от старого low-rank U·Vᵀ, Cayley гарантирует ортогональность аналитически (не требует norm clip и orth_loss). R использует формулу Вудбери для O(D²r) forward (3% оверхед, r=8). Экспериментально подтверждено: R^T·R = I с точностью machine epsilon.

3. **K=4 — достаточный минимум.** Четыре спектральных моды (λ=1.618, 1.839, 1.928, 1.966) покрывают необходимый диапазон частот. K=8 быстрее, но разница не драматична.

4. **Гейты — routing, а не кодирование.** 4 gate-значения не могут линейно предсказать h (R² < 0). Они выбирают режим работы, а не кодируют состояние. Это фундаментально отличается от механизма attention в трансформерах.

5. **Интерполяция — нелинейна.** Смесь двух контекстов даёт резкий, а не плавный переход (KL=0.8 при α=0.5). λ_d работает как нелинейный фильтр с пороговым поведением.

6. **Токенизатор оптимален.** Наш 50K BTE, обученный на русской Wikipedia, даёт в 1.7× лучшую компрессию русского текста, чем Qwen3 (146K) или Mistral Nemo (131K).

---

## 13. Adaptive Depth: routing по важности токена (добавлено в версии с экспериментом)

### Идея
Не все токены требуют одинаковой глубины обработки. Синтаксические токены (союзы, предлоги, пунктуация) достаточно обработать поверхностно. Смысловые токены (существительные, глаголы, именованные сущности) — глубоко.

### Механизм
- Gate spread `σ(α) = std(α₁, …, α_K)` — мера решительности модели при выборе моды
- Высокий spread → модель чётко выбрала моду → токен важен → продолжает на следующий слой
- Низкий spread → модель не уверена (равномерное распределение) → токен заморожен

### Реализация в `LDStack.forward()`
```
Training:   h = σ(α)·h_mlp + (1-σ(α))·h_old    (soft, differentiable)
Inference:  h = h_mlp если σ(α) > τ, иначе h    (hard, zero-cost)
```
Пороги τₗ = sigmoid(depth_logits[l]) — обучаемые параметры, инициализированы линейно от `depth_threshold_low` до `depth_threshold_high`.

### Эмпирические значения (step 25000, 12 слоёв, D=896)
- Mean spread по всем слоям: **0.45** (натренированная модель) vs **0.13** (случайная)
- При порогах 0.08-0.20: 99% токенов проходят все слои (пороги слишком низкие)
- Рекомендуемые пороги: **0.25-0.45** для текущего состояния модели

### Абстракционные уровни (по spread, 4 уровня)

| Уровень | Spread | Режим | Доминантная мода |
|:-------:|:------:|-------|:----------------:|
| L0 (синтаксис) | < 0.08 | Равномерное распределение | m1/m4 (~40% каждая) |
| L1 (модификаторы) | 0.08-0.15 | Слабый выбор | m4 ~33% |
| L2 (конкретные) | 0.15-0.25 | Уверенный выбор | m4 ~41% |
| L3 (абстрактные) | > 0.25 | Решительный выбор | m4 ~60% (93% доминанта) |

Ключевое наблюдение: **абстрактность коррелирует с m4** (глобальный контекст, λ → 1.966). Чем важнее токен, тем сильнее он требует доступа к дальнему контексту.

### Интерфейс
```python
cfg.adaptive_depth = True
cfg.depth_threshold_low = 0.25
cfg.depth_threshold_high = 0.45

stack = LDStack(cfg)
h, gates = stack(h, return_gates=True)        # adaptive: auto by spread
h = stack(h, force_depth=depth_per_token)     # force: explicit depth per token (0..n_layers)
```

### Статус
- ✅ Core implementation in `LDStack`
- ✅ Soft (trainable) + hard (inference) routing
- ✅ Backward compatible (enabled by default)
- ✅ force_depth API for external routing control
- ✅ Интегрировано в `train_phase2.py` и `colab_train.py` (Cayley + |A+B| лог)

---

## 14. Learnable V: поворот Кэли на кососимметрическом генераторе

### Мотивация
V_frozen — ортогональный координатор, общий для всех слоёв. Но разные слои могут хотеть разные базисы: ранние — для локальной грамматики, поздние — для глобальной абстракции. Вместо разморозки полной D×D матрицы (9.6M параметров на 12 слоёв) добавлен **поворот Кэли** — минимальный ортогональный апдейт.

### Проблема low-rank U·Vᵀ
Предыдущая реализация `V_eff = V_frozen + U·Vᵀ` требовала norm clip (0.3) для удержания ортогональности. На глубине 36+ слоёв неортогональность накапливается: `V_eff⁻¹ ≠ V_effᵀ`, что нарушает спектральное преобразование. orth_loss смягчал, но не устранял проблему.

### Решение: Cayley на кососимметрическом генераторе

```
S = A·B^T - B·A^T    (кососимметрическая, rank 2r)
R = (I - S)^{-1} @ (I + S)    (Cayley transform → R ∈ O(D))
V_eff = V_frozen @ R
```

**Свойства:**
- R ∈ O(D) при **любых** A, B (аналитически, machine epsilon)
- S = A·B^T - B·A^T — кососимметрическая по построению (S^T = -S)
- Размер: D×2r = 896×16 = 14.3K параметров на слой (vs 803K для полной V)

### Явное вычисление R (прямой solve D×D)

R вычисляется один раз на forward через `torch.linalg.solve(I - S, I + S)` — `O(D³)`.

```
S = A·B^T - B·A^T          # (D,D), rank 2r, skew-symmetric
R = solve(I - S, I + S)    # (D,D) — Cayley → R ∈ O(D)
V_eff   = V_frozen @ R      # (D,D) — кешируется на весь forward
V_eff^T = R^T @ V_frozen^T  # (D,D) — кешируется на весь forward
```

**Стоимость:** solve 896×896 ~ 240M FLOPs + два матмула V@R и R^T@V^T ~ 1.4B FLOPs — сумма ~1.7B FLOPs, ~9ms на T4. На фоне батч-матмулов (B×L×D) overhead ~2× против Woodbury, но **ортогональность — machine epsilon** (`||V_eff V_eff^T - I||_F / D < 1×10⁻⁵` на всех 12 слоях экспериментально).

### Почему не Woodbury

Предыдущая реализация использовала Woodbury (`O(D²r)`) для применения R к каждому токену без явного построения матрицы. Эксперименты показали:

| Метод | ||V_eff V_eff^T - I|| | Overhead |
|-------|:---------------------:|:--------:|
| Woodbury (2r=16) | ~3×10⁻³ (0.3%) | ~3% |
| Явный solve (D=896) | **~5×10⁻⁷** | ~2× |

Ошибка Woodbury в `0.3%` накапливается через 12+ слоёв. Явный solve даёт машинную точность и упрощает код.

### Пайплайн

```
Forward (V_eff^T):  h_proj = h_norm @ V_eff^T
Inverse (V_eff):    delta = h_scaled @ V_eff
```

Два матмула, никаких дополнительных вызовов solve на токен.

### Что удалено
- `V_delta_U`, `V_delta_V` (заменены на `V_cay_A`, `V_cay_B`)
- `clip_v_delta()` — не нужен (Cayley гарантирует ортогональность)
- `orth_loss()` — не нужен (ортогональность аналитическая)
- `V_delta_max_norm` из конфига
- `cayley_apply()` — заменён на `compute_R()` + прямые matmul

### Связка с adaptive depth
```
adaptive_depth -> какие токены важны (routing по spread)
learnable_V    -> как их обрабатывать (базис под абстракцию)
```
Два механизма без хардкода: модель сама решает, какие токены обрабатывать глубже, и сама настраивает ортогональный поворот под свою глубину.

## 15. Эксперимент: ZeckendorfReadout (закрыт)

**Задача:** Заменить lm_head (44.8M, 94% параметров инференса) на древовидный декодер Фибоначчи (86K = 521× меньше).

**Результаты:**
| Тест | lm_head PPL | Zeckendorf PPL | Условия |
|------|-------------|----------------|---------|
| Frozen stack, 200 steps | 32,652 | **1,435** | Только ZK тренируется |
| Co-training 1 epoch | **32,651** | 6.25×10¹⁸ | Stack + ZK совместно |

ZK побеждает на замороженном stack (находит более эффективную проекцию), но при co-training градиенты через ZK шумны → stack уходит в сторону → ZK не догоняет. lm_head — 0.04% модели, overhead незначителен.

**Решение:** Код и чекпоинты удалены. Боевой выход — lm_head.

## 16. Неотвеченные вопросы (будущие фазы)

1. **Масштабирование.** Лучше ли lambda_d scaling law на большом размере (1B+)?
2. **Multi-agent / Subspace Multiplexing.** Можно ли разделить V для параллельных агентов?
3. **Оптимальный K при D=2048+.** При большей ёмкости сети даёт ли K=8 значимое преимущество?
4. **Spectral gates без bootstrapping.** Можно ли заменить learned gates на спектральные (V·h^2), если V тоже учится?
5. **Adaptive depth tuning.** Как оптимально настраивать thresholds под конкретный PPL? Есть ли корреляция spread -> глубина на дообученной модели?
6. **Cayley rank vs depth.** Эффективный ранг R растёт с глубиной? Нужно ли увеличивать r=8 для 36+ слоёв?
7. **Cross-layer R sharing.** Имеет ли смысл общий Cayley поворот для групп слоёв + per-group projection?
